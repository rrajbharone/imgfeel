---
import MainLayout from '@/layouts/MainLayout.astro';
import Breadcrumbs from '@/components/Breadcrumbs.astro';
import VideoRotator from '@/components/VideoRotator.astro';
import VideoRotatorContent from '@/components/VideoRotatorContent.astro';
import SEOHead from '@/components/SEOHead.astro';
import { t } from '@/i18n/utils';
import { getToolUrl } from '@/data/tools';

const locale = 'en';
const toolUrl = getToolUrl('video-rotator', locale);
---

<MainLayout
  locale={locale}
  title={t(locale, 'vidrot.meta.title')}
  description={t(locale, 'vidrot.meta.description')}
  canonicalUrl={`https://imgfeel.com${toolUrl}`}
  basePath={toolUrl}
>
  <Fragment slot="head">
    <SEOHead schema={{
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "Video Rotator",
      "url": `https://imgfeel.com${toolUrl}`,
      "applicationCategory": "MultimediaApplication",
      "operatingSystem": "Any",
      "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" }
    }} />
  </Fragment>

  <div class="section">
    <div class="container">
      <Breadcrumbs locale={locale} items={[
        { label: t(locale, 'breadcrumb.home'), href: '/' },
        { label: t(locale, 'breadcrumb.tools'), href: '/tools/' },
        { label: t(locale, 'vidrot.hero.title') }
      ]} />
      
      <div class="tool-header">
        <h1 class="tool-title">{t(locale, 'vidrot.hero.title')}</h1>
        <p class="tool-subtitle">{t(locale, 'vidrot.hero.subtitle')}</p>
      </div>

      <VideoRotator locale={locale} />
    </div>
  </div>

  <VideoRotatorContent locale={locale} />
</MainLayout>

<style>
  .tool-header {
    text-align: center;
    max-width: var(--container-narrow);
    margin-inline: auto;
    margin-top: var(--space-4);
    margin-bottom: var(--space-6);
  }

  .tool-title {
    font-size: var(--text-2xl);
    font-weight: 800;
    color: var(--color-text);
    margin: 0 0 var(--space-2) 0;
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  @media (min-width: 640px) {
    .tool-title {
      font-size: var(--text-3xl);
    }
  }

  .tool-subtitle {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: 1.5;
    margin: 0;
  }

  @media (min-width: 640px) {
    .tool-subtitle {
      font-size: var(--text-base);
    }
  }
</style>
