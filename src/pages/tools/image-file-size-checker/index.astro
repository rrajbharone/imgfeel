---
import MainLayout from '@/layouts/MainLayout.astro';
import Breadcrumbs from '@/components/Breadcrumbs.astro';
import FileSizeChecker from '@/components/FileSizeChecker.astro';
import FileSizeCheckerContent from '@/components/FileSizeCheckerContent.astro';
import SEOHead from '@/components/SEOHead.astro';
import { t } from '@/i18n/utils';
import { getToolUrl } from '@/data/tools';

const locale = 'en';
const toolUrl = getToolUrl('image-file-size-checker', locale);
---

<MainLayout
  locale={locale}
  title={t(locale, 'sizecheck.meta.title')}
  description={t(locale, 'sizecheck.meta.description')}
  canonicalUrl={`https://imgfeel.com${toolUrl}`}
  basePath={toolUrl}
>
  <Fragment slot="head">
    <SEOHead schema={{
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "Image File Size Checker",
      "url": `https://imgfeel.com${toolUrl}`,
      "applicationCategory": "MultimediaApplication",
      "operatingSystem": "Any",
      "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" }
    }} />
  </Fragment>

  <div class="section">
    <div class="container">
      <Breadcrumbs locale={locale} items={[
        { label: t(locale, 'breadcrumb.home'), href: '/' },
        { label: t(locale, 'breadcrumb.tools'), href: '/tools/' },
        { label: t(locale, 'sizecheck.hero.title') }
      ]} />
      
      <div class="tool-header">
        <h1 class="tool-title">{t(locale, 'sizecheck.hero.title')}</h1>
        <p class="tool-subtitle">{t(locale, 'sizecheck.hero.subtitle')}</p>
      </div>

      <FileSizeChecker locale={locale} />
    </div>
  </div>

  <FileSizeCheckerContent locale={locale} />
</MainLayout>

<style>
  .tool-header {
    text-align: center;
    max-width: var(--container-narrow);
    margin-inline: auto;
    margin-top: var(--space-4);
    margin-bottom: var(--space-6);
  }
  @media (min-width: 640px) {
    .tool-header {
      margin-top: var(--space-6);
      margin-bottom: var(--space-8);
    }
  }
  .tool-title {
    font-size: var(--text-2xl);
    font-weight: 800;
    color: var(--color-text);
    margin-bottom: var(--space-2);
    letter-spacing: -0.02em;
    line-height: 1.25;
  }
  @media (min-width: 640px) {
    .tool-title {
      font-size: var(--text-3xl);
    }
  }
  .tool-subtitle {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: 1.5;
  }
  @media (min-width: 640px) {
    .tool-subtitle {
      font-size: var(--text-base);
    }
  }
</style>
