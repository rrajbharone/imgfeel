---
import MainLayout from '@/layouts/MainLayout.astro';
import Breadcrumbs from '@/components/Breadcrumbs.astro';
import SeoFilenameGenerator from '@/components/SeoFilenameGenerator.astro';
import SeoFilenameGeneratorContent from '@/components/SeoFilenameGeneratorContent.astro';
import SEOHead from '@/components/SEOHead.astro';
import { t } from '@/i18n/utils';
import { getToolUrl } from '@/data/tools';

const locale = 'en';
const toolUrl = getToolUrl('seo-image-filename-generator', locale);
---

<MainLayout
  locale={locale}
  title={t(locale, 'seoFilename.meta.title')}
  description={t(locale, 'seoFilename.meta.description')}
  canonicalUrl={`https://imgfeel.com${toolUrl}`}
  basePath={toolUrl}
>
  <Fragment slot="head">
    <SEOHead schema={{
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "SEO Image Filename Generator",
      "url": `https://imgfeel.com${toolUrl}`,
      "applicationCategory": "MultimediaApplication",
      "operatingSystem": "Any",
      "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" }
    }} />
  </Fragment>

  <div class="section">
    <div class="container">
      <Breadcrumbs locale={locale} items={[
        { label: t(locale, 'breadcrumb.home'), href: '/' },
        { label: t(locale, 'breadcrumb.tools'), href: '/tools/' },
        { label: t(locale, 'seoFilename.hero.title') }
      ]} />
      
      <div class="tool-header">
        <h1 class="tool-title">{t(locale, 'seoFilename.hero.title')}</h1>
        <p class="tool-subtitle">{t(locale, 'seoFilename.hero.subtitle')}</p>
      </div>

      <SeoFilenameGenerator locale={locale} />
    </div>
  </div>

  <SeoFilenameGeneratorContent locale={locale} />
</MainLayout>

<style>
  .tool-header {
    text-align: center;
    max-width: var(--container-narrow);
    margin-inline: auto;
    margin-top: var(--space-4);
    margin-bottom: var(--space-6);
  }

  .tool-title {
    font-size: var(--text-3xl);
    font-weight: 800;
    color: var(--color-text);
    line-height: var(--leading-tight);
    margin-bottom: var(--space-2);
  }

  .tool-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: var(--leading-normal);
  }

  @media (min-width: 640px) {
    .tool-title {
      font-size: var(--text-4xl);
    }
    
    .tool-subtitle {
      font-size: var(--text-lg);
    }
  }
</style>
