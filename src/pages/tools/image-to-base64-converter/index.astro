---
import MainLayout from '@/layouts/MainLayout.astro';
import Breadcrumbs from '@/components/Breadcrumbs.astro';
import Base64Converter from '@/components/Base64Converter.astro';
import Base64Content from '@/components/Base64Content.astro';
import SEOHead from '@/components/SEOHead.astro';
import { t } from '@/i18n/utils';
import { getToolUrl } from '@/data/tools';

const locale = 'en';
const toolUrl = getToolUrl('image-to-base64-converter', locale);
---

<MainLayout
  locale={locale}
  title={t(locale, 'b64.meta.title')}
  description={t(locale, 'b64.meta.description')}
  canonicalUrl={`https://imgfeel.com${toolUrl}`}
  basePath={toolUrl}
>
  <Fragment slot="head">
    <SEOHead schema={{
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "Image to Base64 Converter",
      "url": `https://imgfeel.com${toolUrl}`,
      "applicationCategory": "DeveloperApplication",
      "operatingSystem": "Any",
      "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" }
    }} />
  </Fragment>

  <div class="section">
    <div class="container">
      <Breadcrumbs locale={locale} items={[
        { label: t(locale, 'breadcrumb.home'), href: '/' },
        { label: t(locale, 'breadcrumb.tools'), href: '/tools/' },
        { label: t(locale, 'b64.hero.title') }
      ]} />
      
      <div class="tool-header">
        <h1 class="tool-title">{t(locale, 'b64.hero.title')}</h1>
        <p class="tool-subtitle">{t(locale, 'b64.hero.subtitle')}</p>
      </div>

      <Base64Converter locale={locale} />
    </div>
  </div>

  <Base64Content locale={locale} />
</MainLayout>

<style>
  .tool-header {
    text-align: center;
    max-width: var(--container-narrow);
    margin-inline: auto;
    margin-top: var(--space-6);
    margin-bottom: var(--space-8);
  }
  .tool-title {
    font-size: var(--text-3xl);
    font-weight: 800;
    color: var(--color-text);
    margin-bottom: var(--space-2);
  }
  .tool-subtitle {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
  }
</style>
