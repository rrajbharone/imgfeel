---
import { t } from '@/i18n/utils';
import type { Locale } from '@/i18n/config';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": t(locale, 'mosaic.faq.q1'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'mosaic.faq.a1')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'mosaic.faq.q2'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'mosaic.faq.a2')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'mosaic.faq.q3'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'mosaic.faq.a3')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'mosaic.faq.q4'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'mosaic.faq.a4')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'mosaic.faq.q5'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'mosaic.faq.a5')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'mosaic.faq.q6'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'mosaic.faq.a6')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'mosaic.faq.q7'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'mosaic.faq.a7')
      }
    }
  ]
};
---

<section class="section content-section">
  <div class="container">
    <div class="content-body">
      <!-- Section 1 -->
      <h2>{t(locale, 'mosaic.content.h1')}</h2>
      <p>{t(locale, 'mosaic.content.p1a')}</p>
      <p>{t(locale, 'mosaic.content.p1b')}</p>

      <div class="feature-cards-grid">
        <div class="feature-card">
          <div class="feature-card-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="3" y="3" width="7" height="7" rx="1"></rect>
              <rect x="14" y="3" width="7" height="7" rx="1"></rect>
              <rect x="14" y="14" width="7" height="7" rx="1"></rect>
              <rect x="3" y="14" width="7" height="7" rx="1"></rect>
            </svg>
          </div>
          <h3>{t(locale, 'mosaic.content.feat1Title')}</h3>
          <p>{t(locale, 'mosaic.content.feat1Desc')}</p>
        </div>

        <div class="feature-card">
          <div class="feature-card-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polygon points="12 2 2 7 12 12 22 7 12 2"></polygon>
              <polyline points="2 17 12 22 22 17"></polyline>
              <polyline points="2 12 12 17 22 12"></polyline>
            </svg>
          </div>
          <h3>{t(locale, 'mosaic.content.feat2Title')}</h3>
          <p>{t(locale, 'mosaic.content.feat2Desc')}</p>
        </div>

        <div class="feature-card">
          <div class="feature-card-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="9" stroke-dasharray="2 3"></circle>
              <circle cx="12" cy="12" r="5"></circle>
            </svg>
          </div>
          <h3>{t(locale, 'mosaic.content.feat3Title')}</h3>
          <p>{t(locale, 'mosaic.content.feat3Desc')}</p>
        </div>

        <div class="feature-card">
          <div class="feature-card-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
            </svg>
          </div>
          <h3>{t(locale, 'mosaic.content.feat4Title')}</h3>
          <p>{t(locale, 'mosaic.content.feat4Desc')}</p>
        </div>
      </div>

      <!-- Section 2: Popular Use Cases -->
      <h2>{t(locale, 'mosaic.content.h2')}</h2>
      <p>{t(locale, 'mosaic.content.p2a')}</p>
      <ul>
        <li><strong>{t(locale, 'mosaic.content.use1Title')}:</strong> {t(locale, 'mosaic.content.use1Desc')}</li>
        <li><strong>{t(locale, 'mosaic.content.use2Title')}:</strong> {t(locale, 'mosaic.content.use2Desc')}</li>
        <li><strong>{t(locale, 'mosaic.content.use3Title')}:</strong> {t(locale, 'mosaic.content.use3Desc')}</li>
        <li><strong>{t(locale, 'mosaic.content.use4Title')}:</strong> {t(locale, 'mosaic.content.use4Desc')}</li>
        <li><strong>{t(locale, 'mosaic.content.use5Title')}:</strong> {t(locale, 'mosaic.content.use5Desc')}</li>
      </ul>

      <!-- Section 3: Modes Comparison -->
      <h2>{t(locale, 'mosaic.content.h3')}</h2>
      <p>{t(locale, 'mosaic.content.p3a')}</p>
      <p>{t(locale, 'mosaic.content.p3b')}</p>

      <div class="table-responsive">
        <table class="data-table">
          <thead>
            <tr>
              <th>Mosaic Style</th>
              <th>How It Operates</th>
              <th>Best Creative Use Case</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><strong>Photo Grid Mosaic</strong></td>
              <td>Arranges multiple uploaded images into an M × N geometric grid</td>
              <td>Event photo albums, portfolio showcases, product collections</td>
            </tr>
            <tr>
              <td><strong>Master Photo Mosaic</strong></td>
              <td>Rebuilds a master portrait using a pool of thumbnail pictures</td>
              <td>Anniversary gifts, memorial tributes, anniversary prints</td>
            </tr>
            <tr>
              <td><strong>Artistic Geometric Mosaic</strong></td>
              <td>Converts single images into stylized squares, dots, hexagons, or diamonds</td>
              <td>Modern wall art, graphic backgrounds, game textures, avatar badges</td>
            </tr>
          </tbody>
        </table>
      </div>

      <!-- Section 4: Grid Layouts & Tile Spacing -->
      <h2>{t(locale, 'mosaic.content.h4')}</h2>
      <p>{t(locale, 'mosaic.content.p4a')}</p>
      <p>{t(locale, 'mosaic.content.p4b')}</p>

      <!-- Section 5: High Resolution & Quality Export -->
      <h2>{t(locale, 'mosaic.content.h5')}</h2>
      <p>{t(locale, 'mosaic.content.p5a')}</p>
      <p>{t(locale, 'mosaic.content.p5b')}</p>

      <!-- Section 6: Step-by-Step Tutorial -->
      <h2>{t(locale, 'mosaic.content.h6')}</h2>
      <ol class="tutorial-steps-list">
        <li><strong>{t(locale, 'mosaic.content.step1Title')}:</strong> {t(locale, 'mosaic.content.step1Desc')}</li>
        <li><strong>{t(locale, 'mosaic.content.step2Title')}:</strong> {t(locale, 'mosaic.content.step2Desc')}</li>
        <li><strong>{t(locale, 'mosaic.content.step3Title')}:</strong> {t(locale, 'mosaic.content.step3Desc')}</li>
        <li><strong>{t(locale, 'mosaic.content.step4Title')}:</strong> {t(locale, 'mosaic.content.step4Desc')}</li>
      </ol>

      <!-- Section 7: FAQ -->
      <div class="faq-container">
        <h2>{t(locale, 'mosaic.faq.heading')}</h2>
        
        <div class="faq-accordion">
          <details class="faq-item" open>
            <summary class="faq-question"><h3>{t(locale, 'mosaic.faq.q1')}</h3></summary>
            <div class="faq-answer"><p>{t(locale, 'mosaic.faq.a1')}</p></div>
          </details>

          <details class="faq-item">
            <summary class="faq-question"><h3>{t(locale, 'mosaic.faq.q2')}</h3></summary>
            <div class="faq-answer"><p>{t(locale, 'mosaic.faq.a2')}</p></div>
          </details>

          <details class="faq-item">
            <summary class="faq-question"><h3>{t(locale, 'mosaic.faq.q3')}</h3></summary>
            <div class="faq-answer"><p>{t(locale, 'mosaic.faq.a3')}</p></div>
          </details>

          <details class="faq-item">
            <summary class="faq-question"><h3>{t(locale, 'mosaic.faq.q4')}</h3></summary>
            <div class="faq-answer"><p>{t(locale, 'mosaic.faq.a4')}</p></div>
          </details>

          <details class="faq-item">
            <summary class="faq-question"><h3>{t(locale, 'mosaic.faq.q5')}</h3></summary>
            <div class="faq-answer"><p>{t(locale, 'mosaic.faq.a5')}</p></div>
          </details>

          <details class="faq-item">
            <summary class="faq-question"><h3>{t(locale, 'mosaic.faq.q6')}</h3></summary>
            <div class="faq-answer"><p>{t(locale, 'mosaic.faq.a6')}</p></div>
          </details>

          <details class="faq-item">
            <summary class="faq-question"><h3>{t(locale, 'mosaic.faq.q7')}</h3></summary>
            <div class="faq-answer"><p>{t(locale, 'mosaic.faq.a7')}</p></div>
          </details>
        </div>
      </div>
    </div>
  </div>

  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
</section>

<style>
  .content-section {
    background: var(--color-surface);
    border-top: 1px solid var(--color-border);
    padding: var(--space-10) 0;
  }

  @media (min-width: 640px) {
    .content-section {
      padding: var(--space-12) 0;
    }
  }

  .content-body {
    max-width: var(--container-narrow);
    margin-inline: auto;
    color: var(--color-text-secondary);
    font-size: var(--text-base);
    line-height: 1.75;
    box-sizing: border-box;
  }

  .content-body h2 {
    color: var(--color-text);
    font-size: var(--text-xl);
    font-weight: 800;
    margin-top: var(--space-8);
    margin-bottom: var(--space-3);
    letter-spacing: -0.02em;
    line-height: 1.3;
    word-break: break-word;
  }

  @media (min-width: 640px) {
    .content-body h2 {
      font-size: var(--text-2xl);
      margin-top: var(--space-10);
      margin-bottom: var(--space-4);
    }
  }

  .content-body h2:first-child {
    margin-top: 0;
  }

  .content-body p {
    margin-bottom: var(--space-4);
  }

  .content-body ul,
  .content-body ol {
    margin-bottom: var(--space-6);
    padding-left: var(--space-5);
  }

  .content-body li {
    margin-bottom: var(--space-2-5);
  }

  .content-body strong {
    color: var(--color-text);
  }

  .feature-cards-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-4);
    margin: var(--space-6) 0 var(--space-8) 0;
  }

  @media (min-width: 640px) {
    .feature-cards-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .feature-card {
    background: var(--color-surface-subtle);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-5);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .feature-card-icon {
    width: 36px;
    height: 36px;
    border-radius: var(--radius-md);
    background: var(--color-accent-light);
    color: var(--color-accent);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .feature-card h3 {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-text);
    margin: 0;
  }

  .feature-card p {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    margin: 0;
    line-height: 1.5;
  }

  .table-responsive {
    overflow-x: auto;
    margin: var(--space-6) 0 var(--space-8) 0;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: var(--color-surface);
  }

  .data-table {
    width: 100%;
    border-collapse: collapse;
    font-size: var(--text-sm);
    text-align: left;
  }

  .data-table th,
  .data-table td {
    padding: var(--space-3) var(--space-4);
    border-bottom: 1px solid var(--color-border-light);
  }

  .data-table th {
    background: var(--color-surface-subtle);
    font-weight: 700;
    color: var(--color-text);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .data-table tr:last-child td {
    border-bottom: none;
  }

  /* FAQ */
  .faq-container {
    margin-top: var(--space-12);
  }

  .faq-accordion {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    margin-top: var(--space-4);
  }

  .faq-item {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-surface-subtle);
    overflow: hidden;
  }

  .faq-question {
    padding: var(--space-4);
    cursor: pointer;
    font-weight: 600;
    user-select: none;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .faq-question::-webkit-details-marker {
    display: none;
  }

  .faq-question h3 {
    margin: 0;
    font-size: var(--text-base);
    color: var(--color-text);
    font-weight: 700;
  }

  .faq-answer {
    padding: 0 var(--space-4) var(--space-4) var(--space-4);
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: 1.6;
  }

  .faq-answer p {
    margin: 0;
  }
</style>
