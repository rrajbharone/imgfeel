---
import { t } from '@/i18n/utils';
import type { Locale } from '@/i18n/config';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": t(locale, 'productWhiteBg.content.faq1Q'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'productWhiteBg.content.faq1A')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'productWhiteBg.content.faq2Q'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'productWhiteBg.content.faq2A')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'productWhiteBg.content.faq3Q'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'productWhiteBg.content.faq3A')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'productWhiteBg.content.faq4Q'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'productWhiteBg.content.faq4A')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'productWhiteBg.content.faq5Q'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'productWhiteBg.content.faq5A')
      }
    },
    {
      "@type": "Question",
      "name": t(locale, 'productWhiteBg.content.faq6Q'),
      "acceptedAnswer": {
        "@type": "Answer",
        "text": t(locale, 'productWhiteBg.content.faq6A')
      }
    }
  ]
};
---

<section class="section content-section">
  <div class="container">
    <div class="content-body">
      <!-- Section 1: Introduction & Overview -->
      <h2>{t(locale, 'productWhiteBg.content.h1')}</h2>
      <p>{t(locale, 'productWhiteBg.content.p1a')}</p>
      <p>{t(locale, 'productWhiteBg.content.p1b')}</p>

      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
              <line x1="3" y1="6" x2="21" y2="6"></line>
              <path d="M16 10a4 4 0 0 1-8 0"></path>
            </svg>
          </div>
          <h3>{t(locale, 'productWhiteBg.content.feat1Title')}</h3>
          <p>{t(locale, 'productWhiteBg.content.feat1Desc')}</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"></circle>
              <path d="m4.93 4.93 4.24 4.24"></path>
              <path d="m14.83 9.17 4.24-4.24"></path>
              <path d="m14.83 14.83 4.24 4.24"></path>
              <path d="m9.17 14.83-4.24 4.24"></path>
            </svg>
          </div>
          <h3>{t(locale, 'productWhiteBg.content.feat2Title')}</h3>
          <p>{t(locale, 'productWhiteBg.content.feat2Desc')}</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polygon points="12 2 2 7 12 12 22 7 12 2"></polygon>
              <polyline points="2 17 12 22 22 17"></polyline>
              <polyline points="2 12 12 17 22 12"></polyline>
            </svg>
          </div>
          <h3>{t(locale, 'productWhiteBg.content.feat3Title')}</h3>
          <p>{t(locale, 'productWhiteBg.content.feat3Desc')}</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
            </svg>
          </div>
          <h3>{t(locale, 'productWhiteBg.content.feat4Title')}</h3>
          <p>{t(locale, 'productWhiteBg.content.feat4Desc')}</p>
        </div>
      </div>

      <!-- Section 2: Marketplace Standards -->
      <h2>{t(locale, 'productWhiteBg.content.h2')}</h2>
      <p>{t(locale, 'productWhiteBg.content.p2a')}</p>

      <div class="types-grid">
        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.mkt1Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.mkt1Desc')}</p>
        </div>

        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.mkt2Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.mkt2Desc')}</p>
        </div>

        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.mkt3Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.mkt3Desc')}</p>
        </div>

        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.mkt4Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.mkt4Desc')}</p>
        </div>
      </div>

      <!-- Section 3: Anatomy of Professional Product Photos -->
      <h2>{t(locale, 'productWhiteBg.content.h3')}</h2>
      <p>{t(locale, 'productWhiteBg.content.p3a')}</p>

      <div class="types-grid">
        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.elem1Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.elem1Desc')}</p>
        </div>

        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.elem2Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.elem2Desc')}</p>
        </div>

        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.elem3Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.elem3Desc')}</p>
        </div>
      </div>

      <!-- Section 4: Practical Studio Photography Tips -->
      <h2>{t(locale, 'productWhiteBg.content.h4')}</h2>
      <p>{t(locale, 'productWhiteBg.content.p4a')}</p>

      <div class="types-grid">
        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.tip1Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.tip1Desc')}</p>
        </div>

        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.tip2Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.tip2Desc')}</p>
        </div>

        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.tip3Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.tip3Desc')}</p>
        </div>

        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.tip4Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.tip4Desc')}</p>
        </div>
      </div>

      <!-- Section 5: Image Formats Compared -->
      <h2>{t(locale, 'productWhiteBg.content.h5')}</h2>
      <p>{t(locale, 'productWhiteBg.content.p5a')}</p>

      <div class="types-grid">
        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.fmt1Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.fmt1Desc')}</p>
        </div>

        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.fmt2Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.fmt2Desc')}</p>
        </div>

        <div class="type-card">
          <h4>{t(locale, 'productWhiteBg.content.fmt3Title')}</h4>
          <p>{t(locale, 'productWhiteBg.content.fmt3Desc')}</p>
        </div>
      </div>

      <!-- Section 6: Step-by-Step Guide -->
      <h2>{t(locale, 'productWhiteBg.content.h6')}</h2>
      <div class="steps-grid">
        <div class="step-card">
          <div class="step-number">1</div>
          <div class="step-content">
            <h4>{t(locale, 'productWhiteBg.content.step1Title')}</h4>
            <p>{t(locale, 'productWhiteBg.content.step1Desc')}</p>
          </div>
        </div>

        <div class="step-card">
          <div class="step-number">2</div>
          <div class="step-content">
            <h4>{t(locale, 'productWhiteBg.content.step2Title')}</h4>
            <p>{t(locale, 'productWhiteBg.content.step2Desc')}</p>
          </div>
        </div>

        <div class="step-card">
          <div class="step-number">3</div>
          <div class="step-content">
            <h4>{t(locale, 'productWhiteBg.content.step3Title')}</h4>
            <p>{t(locale, 'productWhiteBg.content.step3Desc')}</p>
          </div>
        </div>

        <div class="step-card">
          <div class="step-number">4</div>
          <div class="step-content">
            <h4>{t(locale, 'productWhiteBg.content.step4Title')}</h4>
            <p>{t(locale, 'productWhiteBg.content.step4Desc')}</p>
          </div>
        </div>
      </div>

      <!-- Section 7: FAQ -->
      <h2>{t(locale, 'productWhiteBg.content.faqTitle')}</h2>
      <div class="faq-accordion">
        <details class="faq-item">
          <summary class="faq-question">
            <span>{t(locale, 'productWhiteBg.content.faq1Q')}</span>
            <svg class="faq-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6 9 12 15 18 9"></polyline>
            </svg>
          </summary>
          <div class="faq-answer">
            <p>{t(locale, 'productWhiteBg.content.faq1A')}</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">
            <span>{t(locale, 'productWhiteBg.content.faq2Q')}</span>
            <svg class="faq-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6 9 12 15 18 9"></polyline>
            </svg>
          </summary>
          <div class="faq-answer">
            <p>{t(locale, 'productWhiteBg.content.faq2A')}</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">
            <span>{t(locale, 'productWhiteBg.content.faq3Q')}</span>
            <svg class="faq-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6 9 12 15 18 9"></polyline>
            </svg>
          </summary>
          <div class="faq-answer">
            <p>{t(locale, 'productWhiteBg.content.faq3A')}</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">
            <span>{t(locale, 'productWhiteBg.content.faq4Q')}</span>
            <svg class="faq-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6 9 12 15 18 9"></polyline>
            </svg>
          </summary>
          <div class="faq-answer">
            <p>{t(locale, 'productWhiteBg.content.faq4A')}</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">
            <span>{t(locale, 'productWhiteBg.content.faq5Q')}</span>
            <svg class="faq-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6 9 12 15 18 9"></polyline>
            </svg>
          </summary>
          <div class="faq-answer">
            <p>{t(locale, 'productWhiteBg.content.faq5A')}</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">
            <span>{t(locale, 'productWhiteBg.content.faq6Q')}</span>
            <svg class="faq-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6 9 12 15 18 9"></polyline>
            </svg>
          </summary>
          <div class="faq-answer">
            <p>{t(locale, 'productWhiteBg.content.faq6A')}</p>
          </div>
        </details>
      </div>
    </div>
  </div>
</section>

<!-- Structured Data FAQ Schema -->
<script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />

<style>
  .content-section {
    padding-top: var(--space-8);
    padding-bottom: var(--space-12);
  }

  .content-body {
    max-width: 900px;
    margin-inline: auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .content-body h2 {
    font-size: var(--text-2xl);
    font-weight: 800;
    color: var(--color-text);
    margin-top: var(--space-6);
    margin-bottom: var(--space-2);
    letter-spacing: -0.02em;
    line-height: 1.3;
  }

  .content-body p {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.7;
    margin: 0;
  }

  /* Features Grid */
  .features-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-4);
    margin-top: var(--space-2);
  }

  @media (min-width: 640px) {
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .feature-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--space-5);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    box-shadow: var(--shadow-sm);
  }

  .feature-icon {
    width: 44px;
    height: 44px;
    border-radius: var(--radius-lg);
    background: var(--color-accent-light);
    color: var(--color-accent);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: var(--space-1);
  }

  .feature-card h3 {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-text);
    margin: 0;
  }

  .feature-card p {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  /* Types Grid */
  .types-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-3-5);
  }

  @media (min-width: 640px) {
    .types-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .type-card {
    background: var(--color-surface-subtle);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-4);
    display: flex;
    flex-direction: column;
    gap: var(--space-1-5);
  }

  .type-card h4 {
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-text);
    margin: 0;
  }

  .type-card p {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  /* Steps Grid */
  .steps-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .step-card {
    display: flex;
    align-items: flex-start;
    gap: var(--space-4);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-4);
  }

  .step-number {
    width: 32px;
    height: 32px;
    border-radius: var(--radius-full);
    background: var(--color-accent);
    color: #ffffff;
    font-weight: 800;
    font-size: var(--text-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .step-content {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .step-content h4 {
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-text);
    margin: 0;
  }

  .step-content p {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  /* FAQ Accordion */
  .faq-accordion {
    display: flex;
    flex-direction: column;
    gap: var(--space-2-5);
  }

  .faq-item {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    transition: all 0.2s ease;
  }

  .faq-item[open] {
    border-color: var(--color-accent-border);
    background: var(--color-surface);
  }

  .faq-question {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--space-4);
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-text);
    cursor: pointer;
    user-select: none;
    list-style: none;
  }

  .faq-question::-webkit-details-marker {
    display: none;
  }

  .faq-chevron {
    transition: transform 0.2s ease;
    color: var(--color-text-muted);
    flex-shrink: 0;
  }

  .faq-item[open] .faq-chevron {
    transform: rotate(180deg);
    color: var(--color-accent);
  }

  .faq-answer {
    padding: 0 var(--space-4) var(--space-4);
  }

  .faq-answer p {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin: 0;
  }
</style>
