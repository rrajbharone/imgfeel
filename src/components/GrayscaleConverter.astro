---
import { t } from '@/i18n/utils';
import type { Locale } from '@/i18n/config';
import { getToolUrl } from '@/data/tools';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;

const squareUrl = getToolUrl('resize-image-to-1-1', locale);
const circleUrl = getToolUrl('circle-image-cropper', locale);
const webpUrl = getToolUrl('webp-compressor', locale);
const ratioUrl = getToolUrl('image-aspect-ratio-checker', locale);

const i18nDict = {
  statusReady: t(locale, 'grayconv.status.ready'),
  statusProcessing: t(locale, 'grayconv.status.processing'),
  errorLoad: t(locale, 'grayconv.error.load'),
  originalLabel: t(locale, 'grayconv.split.original'),
  grayscaleLabel: t(locale, 'grayconv.split.grayscale'),
  downloadName: t(locale, 'grayconv.download.filename'),
};
---

<div class="gray-tool-wrapper" id="grayscaleApp" data-i18n={JSON.stringify(i18nDict)}>
  <!-- Dropzone / Upload State -->
  <div class="gray-dropzone-card" id="grayDropzone">
    <div class="gray-dropzone-content">
      <div class="gray-icon-circle">
        <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <circle cx="12" cy="12" r="9"></circle>
          <path d="M12 3a9 9 0 0 1 0 18v-18z" fill="currentColor"></path>
        </svg>
      </div>

      <h2 class="gray-drop-title">{t(locale, 'grayconv.dropzone.title')}</h2>
      <p class="gray-drop-subtitle">{t(locale, 'grayconv.dropzone.subtitle')}</p>

      <div class="gray-btn-row">
        <label class="btn btn-primary gray-btn-primary" for="grayFileInput">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
            <polyline points="17 8 12 3 7 8"></polyline>
            <line x1="12" y1="3" x2="12" y2="15"></line>
          </svg>
          <span>{t(locale, 'grayconv.dropzone.browse')}</span>
        </label>
        <input type="file" id="grayFileInput" accept="image/*,.jpg,.jpeg,.png,.webp,.avif,.bmp" class="sr-only" />

        <button type="button" class="btn btn-secondary gray-sample-btn" id="graySampleBtn">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polygon points="12 2 2 7 12 12 22 7 12 2"></polygon>
            <polyline points="2 17 12 22 22 17"></polyline>
            <polyline points="2 12 12 17 22 12"></polyline>
          </svg>
          <span>{t(locale, 'grayconv.dropzone.sample')}</span>
        </button>
      </div>

      <p class="gray-formats-hint">{t(locale, 'grayconv.dropzone.formats')}</p>

      <div class="gray-privacy-badge">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
        </svg>
        <span>{t(locale, 'grayconv.privacy.badge')}</span>
      </div>
    </div>
  </div>

  <!-- Loading State -->
  <div class="gray-loading-card" id="grayLoadingCard" style="display: none;">
    <div class="gray-spinner"></div>
    <p class="gray-loading-text">{t(locale, 'grayconv.status.processing')}</p>
  </div>

  <!-- Main Grayscale Workspace -->
  <div class="gray-workspace" id="grayWorkspace" style="display: none;">
    <!-- Header Bar -->
    <div class="gray-workspace-header">
      <div class="gray-meta-badge">
        <span class="gray-status-dot"></span>
        <span id="grayFileName" class="gray-filename-label">photo.jpg</span>
        <span class="gray-meta-tag" id="grayOriginalDimsTag">1200 × 800 px</span>
        <span class="gray-meta-tag" id="grayOriginalSizeTag">320 KB</span>
      </div>

      <div class="gray-header-actions">
        <button type="button" class="btn btn-secondary btn-sm" id="grayChangeImageBtn">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
            <polyline points="17 8 12 3 7 8"></polyline>
            <line x1="12" y1="3" x2="12" y2="15"></line>
          </svg>
          <span>{t(locale, 'grayconv.action.changeImage')}</span>
        </button>
      </div>
    </div>

    <!-- 2-Column Main Layout: Interactive Preview on Left, Tone Controls on Right -->
    <div class="gray-main-grid">
      <!-- Left Column: Interactive Before/After Preview -->
      <div class="gray-preview-card">
        <!-- View Mode Tabs -->
        <div class="preview-mode-tabs">
          <button type="button" class="preview-tab active" data-view="split" id="viewModeSplitBtn">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <line x1="12" y1="3" x2="12" y2="21"></line>
              <rect x="3" y="3" width="18" height="18" rx="2"></rect>
            </svg>
            <span>{t(locale, 'grayconv.view.split')}</span>
          </button>
          <button type="button" class="preview-tab" data-view="side" id="viewModeSideBtn">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="2" y="3" width="9" height="18" rx="2"></rect>
              <rect x="13" y="3" width="9" height="18" rx="2"></rect>
            </svg>
            <span>{t(locale, 'grayconv.view.side')}</span>
          </button>
          <button type="button" class="preview-tab" data-view="result" id="viewModeResultBtn">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <circle cx="12" cy="12" r="9"></circle>
            </svg>
            <span>{t(locale, 'grayconv.view.result')}</span>
          </button>
        </div>

        <!-- Split Slider Container -->
        <div class="split-viewport-wrapper" id="splitViewportWrapper">
          <div class="split-comparison-box" id="splitComparisonBox">
            <canvas id="grayCanvasUnder" class="split-canvas"></canvas>
            <canvas id="grayCanvasOver" class="split-canvas over-canvas"></canvas>
            
            <div class="split-divider-line" id="splitDividerLine" style="left: 50%;">
              <div class="split-drag-handle" title="Drag to compare">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                  <polyline points="15 18 9 12 15 6"></polyline>
                </svg>
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                  <polyline points="9 18 15 12 9 6"></polyline>
                </svg>
              </div>
            </div>

            <span class="split-tag tag-left">{t(locale, 'grayconv.split.original')}</span>
            <span class="split-tag tag-right">{t(locale, 'grayconv.split.grayscale')}</span>
          </div>
          <p class="split-hint-text">{t(locale, 'grayconv.split.hint')}</p>
        </div>

        <!-- Side-by-Side Container (Hidden by default) -->
        <div class="side-viewport-wrapper" id="sideViewportWrapper" style="display: none;">
          <div class="side-box">
            <span class="side-badge">{t(locale, 'grayconv.split.original')}</span>
            <img id="sideOriginalImg" src="" alt="Original" class="side-img" />
          </div>
          <div class="side-box">
            <span class="side-badge">{t(locale, 'grayconv.split.grayscale')}</span>
            <img id="sideGrayscaleImg" src="" alt="Grayscale" class="side-img" />
          </div>
        </div>

        <!-- Grayscale Only Viewport (Hidden by default) -->
        <div class="single-viewport-wrapper" id="singleViewportWrapper" style="display: none;">
          <img id="singleGrayscaleImg" src="" alt="Grayscale preview" class="single-img" />
        </div>
      </div>

      <!-- Right Column: Grayscale Tone Controls & Export Settings -->
      <div class="gray-controls-panel">
        <!-- Section 1: Presets -->
        <div class="control-group">
          <label class="control-label">{t(locale, 'grayconv.preset.label')}</label>
          <div class="preset-chips-grid">
            <button type="button" class="tone-preset-btn active" data-preset="luminance">
              <strong>{t(locale, 'grayconv.preset.luminance')}</strong>
              <span>{t(locale, 'grayconv.preset.luminanceDesc')}</span>
            </button>
            <button type="button" class="tone-preset-btn" data-preset="high-contrast">
              <strong>{t(locale, 'grayconv.preset.highContrast')}</strong>
              <span>{t(locale, 'grayconv.preset.highContrastDesc')}</span>
            </button>
            <button type="button" class="tone-preset-btn" data-preset="sepia">
              <strong>{t(locale, 'grayconv.preset.sepia')}</strong>
              <span>{t(locale, 'grayconv.preset.sepiaDesc')}</span>
            </button>
            <button type="button" class="tone-preset-btn" data-preset="cool">
              <strong>{t(locale, 'grayconv.preset.cool')}</strong>
              <span>{t(locale, 'grayconv.preset.coolDesc')}</span>
            </button>
            <button type="button" class="tone-preset-btn" data-preset="average">
              <strong>{t(locale, 'grayconv.preset.average')}</strong>
              <span>{t(locale, 'grayconv.preset.averageDesc')}</span>
            </button>
          </div>
        </div>

        <!-- Section 2: Fine-Tuning Sliders -->
        <div class="control-group">
          <div class="slider-header-row">
            <label for="grayIntensitySlider" class="control-label">{t(locale, 'grayconv.slider.intensity')}</label>
            <span id="grayIntensityVal" class="val-pill">100%</span>
          </div>
          <input type="range" id="grayIntensitySlider" min="0" max="100" value="100" step="1" class="gray-range" />
        </div>

        <div class="control-group">
          <div class="slider-header-row">
            <label for="grayBrightnessSlider" class="control-label">{t(locale, 'grayconv.slider.brightness')}</label>
            <span id="grayBrightnessVal" class="val-pill">0</span>
          </div>
          <input type="range" id="grayBrightnessSlider" min="-100" max="100" value="0" step="1" class="gray-range" />
        </div>

        <div class="control-group">
          <div class="slider-header-row">
            <label for="grayContrastSlider" class="control-label">{t(locale, 'grayconv.slider.contrast')}</label>
            <span id="grayContrastVal" class="val-pill">0</span>
          </div>
          <input type="range" id="grayContrastSlider" min="-100" max="100" value="0" step="1" class="gray-range" />
        </div>

        <div class="reset-adjustments-row">
          <button type="button" class="btn btn-secondary btn-sm" id="grayResetAdjustmentsBtn">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"></path>
              <path d="M3 3v5h5"></path>
            </svg>
            <span>{t(locale, 'grayconv.action.resetSliders')}</span>
          </button>
        </div>

        <!-- Section 3: Format & Quality Selection -->
        <div class="control-group">
          <label for="grayFormatSelect" class="control-label">{t(locale, 'grayconv.format.label')}</label>
          <select id="grayFormatSelect" class="gray-select">
            <option value="original" selected>{t(locale, 'grayconv.format.original')}</option>
            <option value="image/jpeg">JPG / JPEG (Standard)</option>
            <option value="image/png">PNG (Lossless / Transparent)</option>
            <option value="image/webp">WebP (Modern Compressed)</option>
          </select>
        </div>

        <!-- Section 4: Live Metrics & Download -->
        <div class="gray-metrics-card">
          <div class="metric-row">
            <span class="metric-label">{t(locale, 'grayconv.metrics.dimensions')}:</span>
            <strong id="grayMetricDims">1200 × 800 px</strong>
          </div>
          <div class="metric-row">
            <span class="metric-label">{t(locale, 'grayconv.metrics.fileWeight')}:</span>
            <strong id="grayMetricWeight">-- KB</strong>
          </div>
          <div class="metric-row">
            <span class="metric-label">{t(locale, 'grayconv.metrics.colorDepth')}:</span>
            <strong>{t(locale, 'grayconv.metrics.eightBitDepth')}</strong>
          </div>
        </div>

        <!-- Single-Click Download Button -->
        <button type="button" class="btn btn-primary gray-download-btn" id="grayDownloadBtn">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
            <polyline points="7 10 12 15 17 10"></polyline>
            <line x1="12" y1="15" x2="12" y2="3"></line>
          </svg>
          <span>{t(locale, 'grayconv.action.download')}</span>
        </button>
      </div>
    </div>

    <!-- Quick Navigation to Related Tools -->
    <div class="gray-cross-links-row">
      <a href={squareUrl} class="btn btn-secondary gray-cross-btn">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect>
        </svg>
        <span>{t(locale, 'grayconv.cross.square')}</span>
      </a>

      <a href={circleUrl} class="btn btn-secondary gray-cross-btn">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <circle cx="12" cy="12" r="9"></circle>
        </svg>
        <span>{t(locale, 'grayconv.cross.circle')}</span>
      </a>

      <a href={webpUrl} class="btn btn-secondary gray-cross-btn">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"></polygon>
        </svg>
        <span>{t(locale, 'grayconv.cross.compress')}</span>
      </a>

      <a href={ratioUrl} class="btn btn-secondary gray-cross-btn">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M6 2v14a2 2 0 0 0 2 2h14"></path>
          <path d="M18 22V8a2 2 0 0 0-2-2H2"></path>
        </svg>
        <span>{t(locale, 'grayconv.cross.ratio')}</span>
      </a>
    </div>
  </div>
</div>

<script>
  import {
    convertImageToGrayscale,
    generateSampleColorImage,
    loadImage,
    applyGrayscaleToImageData,
    type GrayscalePreset,
    type GrayscaleResult,
  } from '@/scripts/grayscale-converter';

  function initGrayscaleApp() {
    const container = document.getElementById('grayscaleApp');
    if (!container) return;

    let i18n = {
      statusReady: 'Ready',
      statusProcessing: 'Converting to Grayscale...',
      errorLoad: 'Failed to process image file',
      downloadName: 'image-grayscale',
    };

    try {
      const dataAttr = container.getAttribute('data-i18n');
      if (dataAttr) i18n = JSON.parse(dataAttr);
    } catch {}

    // Elements
    const dropzone = document.getElementById('grayDropzone');
    const fileInput = document.getElementById('grayFileInput') as HTMLInputElement | null;
    const sampleBtn = document.getElementById('graySampleBtn');
    const loadingCard = document.getElementById('grayLoadingCard');
    const workspace = document.getElementById('grayWorkspace');

    // Header
    const fileNameEl = document.getElementById('grayFileName');
    const origDimsTag = document.getElementById('grayOriginalDimsTag');
    const origSizeTag = document.getElementById('grayOriginalSizeTag');
    const changeImageBtn = document.getElementById('grayChangeImageBtn');

    // View tabs
    const viewTabs = document.querySelectorAll('.preview-tab');
    const splitWrapper = document.getElementById('splitViewportWrapper');
    const sideWrapper = document.getElementById('sideViewportWrapper');
    const singleWrapper = document.getElementById('singleViewportWrapper');

    // Canvas elements for Split View
    const canvasUnder = document.getElementById('grayCanvasUnder') as HTMLCanvasElement | null;
    const canvasOver = document.getElementById('grayCanvasOver') as HTMLCanvasElement | null;
    const splitBox = document.getElementById('splitComparisonBox');
    const splitDivider = document.getElementById('splitDividerLine');

    // Side & Single images
    const sideOrigImg = document.getElementById('sideOriginalImg') as HTMLImageElement | null;
    const sideGrayImg = document.getElementById('sideGrayscaleImg') as HTMLImageElement | null;
    const singleGrayImg = document.getElementById('singleGrayscaleImg') as HTMLImageElement | null;

    // Controls
    const presetBtns = document.querySelectorAll('.tone-preset-btn');
    const intensitySlider = document.getElementById('grayIntensitySlider') as HTMLInputElement | null;
    const intensityVal = document.getElementById('grayIntensityVal');
    const brightnessSlider = document.getElementById('grayBrightnessSlider') as HTMLInputElement | null;
    const brightnessVal = document.getElementById('grayBrightnessVal');
    const contrastSlider = document.getElementById('grayContrastSlider') as HTMLInputElement | null;
    const contrastVal = document.getElementById('grayContrastVal');
    const resetAdjustmentsBtn = document.getElementById('grayResetAdjustmentsBtn');
    const formatSelect = document.getElementById('grayFormatSelect') as HTMLSelectElement | null;

    // Metrics & Download
    const metricDims = document.getElementById('grayMetricDims');
    const metricWeight = document.getElementById('grayMetricWeight');
    const downloadBtn = document.getElementById('grayDownloadBtn');

    // State
    let currentFile: File | null = null;
    let loadedImageObj: HTMLImageElement | null = null;
    let originalWidth = 0;
    let originalHeight = 0;

    let activePreset: GrayscalePreset = 'luminance';
    let intensity = 100;
    let brightness = 0;
    let contrast = 0;
    let splitPosPercent = 50;
    let isDraggingSplit = false;

    let lastResult: GrayscaleResult | null = null;
    let renderDebounceTimer: number | null = null;

    function renderSplitCanvases() {
      if (!canvasUnder || !canvasOver || !loadedImageObj) return;

      const maxDisplayW = 600;
      const aspect = originalHeight / originalWidth;
      const displayW = Math.min(originalWidth, maxDisplayW);
      const displayH = Math.round(displayW * aspect);

      canvasUnder.width = displayW;
      canvasUnder.height = displayH;
      canvasOver.width = displayW;
      canvasOver.height = displayH;

      const ctxUnder = canvasUnder.getContext('2d', { willReadFrequently: true });
      const ctxOver = canvasOver.getContext('2d', { willReadFrequently: true });
      if (!ctxUnder || !ctxOver) return;

      // 1. Draw original color image in Under canvas
      ctxUnder.drawImage(loadedImageObj, 0, 0, displayW, displayH);

      // 2. Draw and process grayscale in Over canvas
      ctxOver.drawImage(loadedImageObj, 0, 0, displayW, displayH);
      const imgData = ctxOver.getImageData(0, 0, displayW, displayH);
      applyGrayscaleToImageData(imgData, {
        preset: activePreset,
        intensity,
        brightness,
        contrast,
        format: 'image/jpeg',
        quality: 0.9,
      });
      ctxOver.putImageData(imgData, 0, 0);

      // Apply clip-path to Over canvas based on splitPosPercent
      canvasOver.style.clipPath = `inset(0 0 0 ${splitPosPercent}%)`;
      if (splitDivider) {
        splitDivider.style.left = `${splitPosPercent}%`;
      }
    }

    async function processFullExport() {
      if (!currentFile) return;

      const format = (formatSelect?.value as 'image/jpeg' | 'image/png' | 'image/webp' | 'original') || 'original';

      try {
        const result = await convertImageToGrayscale(currentFile, {
          preset: activePreset,
          intensity,
          brightness,
          contrast,
          format,
          quality: 0.92,
        });

        lastResult = result;

        if (sideGrayImg) sideGrayImg.src = result.dataUrl;
        if (singleGrayImg) singleGrayImg.src = result.dataUrl;
        if (metricDims) metricDims.textContent = `${result.width} × ${result.height} px`;
        if (metricWeight) metricWeight.textContent = result.fileSizeFormatted;
      } catch (err) {
        console.error('Grayscale export error:', err);
      }
    }

    function triggerUpdate() {
      renderSplitCanvases();
      if (renderDebounceTimer) clearTimeout(renderDebounceTimer);
      renderDebounceTimer = window.setTimeout(processFullExport, 80);
    }

    async function loadFile(file: File) {
      if (!dropzone || !loadingCard || !workspace) return;

      dropzone.style.display = 'none';
      workspace.style.display = 'none';
      loadingCard.style.display = 'flex';

      try {
        currentFile = file;
        loadedImageObj = await loadImage(file);
        originalWidth = loadedImageObj.naturalWidth || loadedImageObj.width;
        originalHeight = loadedImageObj.naturalHeight || loadedImageObj.height;

        // Reset adjustments
        activePreset = 'luminance';
        presetBtns.forEach((b) => b.classList.toggle('active', b.getAttribute('data-preset') === 'luminance'));
        intensity = 100;
        brightness = 0;
        contrast = 0;
        splitPosPercent = 50;

        if (intensitySlider) intensitySlider.value = '100';
        if (intensityVal) intensityVal.textContent = '100%';
        if (brightnessSlider) brightnessSlider.value = '0';
        if (brightnessVal) brightnessVal.textContent = '0';
        if (contrastSlider) contrastSlider.value = '0';
        if (contrastVal) contrastVal.textContent = '0';

        // Header info
        if (fileNameEl) fileNameEl.textContent = file.name;
        if (origDimsTag) origDimsTag.textContent = `${originalWidth} × ${originalHeight} px`;
        if (origSizeTag) origSizeTag.textContent = `${(file.size / 1024).toFixed(1)} KB`;

        const origUrl = URL.createObjectURL(file);
        if (sideOrigImg) sideOrigImg.src = origUrl;

        renderSplitCanvases();
        await processFullExport();

        loadingCard.style.display = 'none';
        workspace.style.display = 'flex';
      } catch (err) {
        alert(err instanceof Error ? err.message : i18n.errorLoad);
        resetToDropzone();
      }
    }

    function resetToDropzone() {
      currentFile = null;
      loadedImageObj = null;
      if (fileInput) fileInput.value = '';
      if (loadingCard) loadingCard.style.display = 'none';
      if (workspace) workspace.style.display = 'none';
      if (dropzone) dropzone.style.display = 'block';
    }

    if (changeImageBtn) changeImageBtn.addEventListener('click', resetToDropzone);

    // View mode switching
    viewTabs.forEach((tab) => {
      tab.addEventListener('click', () => {
        viewTabs.forEach((t) => t.classList.remove('active'));
        tab.classList.add('active');

        const mode = tab.getAttribute('data-view');
        if (splitWrapper) splitWrapper.style.display = mode === 'split' ? 'flex' : 'none';
        if (sideWrapper) sideWrapper.style.display = mode === 'side' ? 'grid' : 'none';
        if (singleWrapper) singleWrapper.style.display = mode === 'result' ? 'block' : 'none';
      });
    });

    // Split dragging logic
    function handleSplitMove(clientX: number) {
      if (!splitBox) return;
      const rect = splitBox.getBoundingClientRect();
      const x = Math.max(0, Math.min(rect.width, clientX - rect.left));
      splitPosPercent = Math.round((x / rect.width) * 100);

      if (splitDivider) splitDivider.style.left = `${splitPosPercent}%`;
      if (canvasOver) canvasOver.style.clipPath = `inset(0 0 0 ${splitPosPercent}%)`;
    }

    if (splitBox) {
      splitBox.addEventListener('mousedown', (e) => {
        isDraggingSplit = true;
        handleSplitMove(e.clientX);
      });

      window.addEventListener('mousemove', (e) => {
        if (!isDraggingSplit) return;
        handleSplitMove(e.clientX);
      });

      window.addEventListener('mouseup', () => {
        isDraggingSplit = false;
      });

      // Touch events for mobile
      splitBox.addEventListener('touchstart', (e) => {
        if (e.touches.length === 1) {
          isDraggingSplit = true;
          handleSplitMove(e.touches[0].clientX);
        }
      }, { passive: true });

      splitBox.addEventListener('touchmove', (e) => {
        if (!isDraggingSplit || e.touches.length !== 1) return;
        handleSplitMove(e.touches[0].clientX);
      }, { passive: true });

      splitBox.addEventListener('touchend', () => {
        isDraggingSplit = false;
      });
    }

    // Presets
    presetBtns.forEach((btn) => {
      btn.addEventListener('click', () => {
        presetBtns.forEach((b) => b.classList.remove('active'));
        btn.classList.add('active');
        activePreset = (btn.getAttribute('data-preset') as GrayscalePreset) || 'luminance';
        triggerUpdate();
      });
    });

    // Sliders
    if (intensitySlider) {
      intensitySlider.addEventListener('input', () => {
        intensity = parseInt(intensitySlider.value, 10);
        if (intensityVal) intensityVal.textContent = `${intensity}%`;
        triggerUpdate();
      });
    }

    if (brightnessSlider) {
      brightnessSlider.addEventListener('input', () => {
        brightness = parseInt(brightnessSlider.value, 10);
        if (brightnessVal) brightnessVal.textContent = `${brightness > 0 ? '+' : ''}${brightness}`;
        triggerUpdate();
      });
    }

    if (contrastSlider) {
      contrastSlider.addEventListener('input', () => {
        contrast = parseInt(contrastSlider.value, 10);
        if (contrastVal) contrastVal.textContent = `${contrast > 0 ? '+' : ''}${contrast}`;
        triggerUpdate();
      });
    }

    if (resetAdjustmentsBtn) {
      resetAdjustmentsBtn.addEventListener('click', () => {
        intensity = 100;
        brightness = 0;
        contrast = 0;
        if (intensitySlider) intensitySlider.value = '100';
        if (intensityVal) intensityVal.textContent = '100%';
        if (brightnessSlider) brightnessSlider.value = '0';
        if (brightnessVal) brightnessVal.textContent = '0';
        if (contrastSlider) contrastSlider.value = '0';
        if (contrastVal) contrastVal.textContent = '0';
        triggerUpdate();
      });
    }

    // Format select
    if (formatSelect) {
      formatSelect.addEventListener('change', triggerUpdate);
    }

    // Download Button
    if (downloadBtn) {
      downloadBtn.addEventListener('click', () => {
        if (!lastResult) return;

        let ext = '.jpg';
        const selFormat = formatSelect?.value || 'original';
        if (selFormat === 'image/png') ext = '.png';
        else if (selFormat === 'image/webp') ext = '.webp';
        else if (selFormat === 'image/jpeg') ext = '.jpg';
        else if (currentFile) {
          const match = currentFile.name.match(/\.[^/.]+$/);
          if (match) ext = match[0];
        }

        const baseName = currentFile ? currentFile.name.replace(/\.[^/.]+$/, '') : 'image';
        const filename = `${baseName}-grayscale${ext}`;

        const a = document.createElement('a');
        a.href = lastResult.dataUrl;
        a.download = filename;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
      });
    }

    // Sample button
    if (sampleBtn) {
      sampleBtn.addEventListener('click', async () => {
        const file = await generateSampleColorImage();
        loadFile(file);
      });
    }

    // File input change
    if (fileInput) {
      fileInput.addEventListener('change', (e) => {
        const target = e.target as HTMLInputElement;
        if (target.files && target.files[0]) {
          loadFile(target.files[0]);
        }
      });
    }

    // Drag and drop
    if (dropzone) {
      ['dragenter', 'dragover'].forEach((eventName) => {
        dropzone.addEventListener(eventName, (e) => {
          e.preventDefault();
          e.stopPropagation();
          dropzone.classList.add('gray-drag-active');
        });
      });

      ['dragleave', 'drop'].forEach((eventName) => {
        dropzone.addEventListener(eventName, (e) => {
          e.preventDefault();
          e.stopPropagation();
          dropzone.classList.remove('gray-drag-active');
        });
      });

      dropzone.addEventListener('drop', (e) => {
        const dt = e.dataTransfer;
        if (dt && dt.files && dt.files[0]) {
          loadFile(dt.files[0]);
        }
      });
    }

    // Paste listener (Ctrl+V)
    window.addEventListener('paste', (e: ClipboardEvent) => {
      const items = e.clipboardData?.items;
      if (!items) return;
      for (let i = 0; i < items.length; i++) {
        if (items[i].type.indexOf('image') !== -1) {
          const file = items[i].getAsFile();
          if (file) {
            loadFile(file);
            break;
          }
        }
      }
    });
  }

  initGrayscaleApp();
  document.addEventListener('astro:page-load', initGrayscaleApp);
</script>

<style>
  .gray-tool-wrapper {
    width: 100%;
    max-width: var(--container-max);
    margin-inline: auto;
    box-sizing: border-box;
    text-align: left;
  }

  /* Dropzone */
  .gray-dropzone-card {
    background: var(--color-surface);
    border: 2px dashed var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--space-8) var(--space-4);
    text-align: center;
    transition: border-color 0.2s ease, background 0.2s ease;
    box-shadow: var(--shadow-sm);
    width: 100%;
    box-sizing: border-box;
  }

  @media (min-width: 640px) {
    .gray-dropzone-card {
      padding: var(--space-12) var(--space-6);
    }
  }

  .gray-dropzone-card.gray-drag-active {
    border-color: var(--color-accent);
    background: var(--color-accent-light);
  }

  .gray-dropzone-content {
    max-width: 580px;
    margin-inline: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-3);
    width: 100%;
    box-sizing: border-box;
  }

  .gray-icon-circle {
    width: 56px;
    height: 56px;
    border-radius: var(--radius-full);
    background: var(--color-accent-light);
    color: var(--color-accent);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: var(--space-2);
  }

  @media (min-width: 640px) {
    .gray-icon-circle {
      width: 64px;
      height: 64px;
    }
  }

  .gray-drop-title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--color-text);
    margin: 0;
    line-height: 1.3;
  }

  @media (min-width: 640px) {
    .gray-drop-title {
      font-size: var(--text-xl);
    }
  }

  .gray-drop-subtitle {
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
    margin: 0;
    line-height: 1.5;
    max-width: 480px;
  }

  @media (min-width: 640px) {
    .gray-drop-subtitle {
      font-size: var(--text-sm);
    }
  }

  .gray-btn-row {
    display: flex;
    flex-direction: column;
    gap: var(--space-2-5);
    width: 100%;
    max-width: 440px;
    margin-top: var(--space-2);
  }

  @media (min-width: 480px) {
    .gray-btn-row {
      flex-direction: row;
      justify-content: center;
    }
  }

  .gray-btn-primary {
    cursor: pointer;
    min-height: 42px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 0 var(--space-5);
  }

  .gray-sample-btn {
    min-height: 42px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 0 var(--space-4);
    font-size: var(--text-xs);
  }

  .gray-formats-hint {
    font-size: 11px;
    color: var(--color-text-muted);
    margin: 0;
    line-height: 1.4;
  }

  .gray-privacy-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: var(--color-surface-subtle);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-full);
    padding: 4px 12px;
    font-size: 11px;
    color: var(--color-text-secondary);
    font-weight: 500;
    margin-top: var(--space-2);
  }

  /* Loading State */
  .gray-loading-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--space-12) var(--space-6);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-4);
    box-shadow: var(--shadow-sm);
    width: 100%;
    box-sizing: border-box;
  }

  .gray-spinner {
    width: 40px;
    height: 40px;
    border: 3px solid var(--color-accent-light);
    border-top-color: var(--color-accent);
    border-radius: 50%;
    animation: graySpin 0.8s linear infinite;
  }

  @keyframes graySpin {
    to {
      transform: rotate(360deg);
    }
  }

  .gray-loading-text {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-text-secondary);
    margin: 0;
  }

  /* Workspace */
  .gray-workspace {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    width: 100%;
    box-sizing: border-box;
  }

  @media (min-width: 640px) {
    .gray-workspace {
      gap: var(--space-5);
    }
  }

  /* Header */
  .gray-workspace-header {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-3) var(--space-4);
    width: 100%;
    box-sizing: border-box;
  }

  @media (min-width: 640px) {
    .gray-workspace-header {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }
  }

  .gray-meta-badge {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
  }

  .gray-status-dot {
    width: 8px;
    height: 8px;
    border-radius: var(--radius-full);
    background: #10b981;
    box-shadow: 0 0 0 2px #d1fae5;
  }

  .gray-filename-label {
    font-size: var(--text-xs);
    font-weight: 700;
    color: var(--color-text);
    max-width: 200px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  @media (min-width: 480px) {
    .gray-filename-label {
      max-width: 320px;
    }
  }

  .gray-meta-tag {
    font-size: 11px;
    font-weight: 600;
    color: var(--color-text-secondary);
    background: var(--color-surface-subtle);
    border: 1px solid var(--color-border-light);
    border-radius: var(--radius-sm);
    padding: 2px 6px;
  }

  .gray-header-actions {
    display: flex;
    gap: var(--space-2);
  }

  /* 2-Column Grid */
  .gray-main-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-4);
    width: 100%;
    box-sizing: border-box;
  }

  @media (min-width: 900px) {
    .gray-main-grid {
      grid-template-columns: 1.15fr 0.85fr;
      gap: var(--space-5);
    }
  }

  /* Preview Card (Left Column) */
  .gray-preview-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--space-4);
    box-shadow: var(--shadow-sm);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    box-sizing: border-box;
  }

  @media (min-width: 640px) {
    .gray-preview-card {
      padding: var(--space-5);
    }
  }

  .preview-mode-tabs {
    display: flex;
    gap: var(--space-2);
    border-bottom: 1px solid var(--color-border-light);
    padding-bottom: var(--space-3);
  }

  .preview-tab {
    background: var(--color-surface-subtle);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: 6px 12px;
    font-size: 11.5px;
    font-weight: 600;
    color: var(--color-text-secondary);
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    transition: all 0.15s ease;
  }

  .preview-tab.active {
    background: var(--color-accent-light);
    border-color: var(--color-accent);
    color: var(--color-accent);
  }

  /* Split Viewport */
  .split-viewport-wrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
    width: 100%;
  }

  .split-comparison-box {
    width: 100%;
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: repeating-conic-gradient(#f1f5f9 0% 25%, #ffffff 0% 50%) 50% / 16px 16px;
    border: 1px solid var(--color-border);
    cursor: ew-resize;
    user-select: none;
    touch-action: none;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  }

  .split-canvas {
    width: 100%;
    height: auto;
    display: block;
  }

  .split-canvas.over-canvas {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  .split-divider-line {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 2px;
    background: #ffffff;
    box-shadow: 0 0 4px rgba(0, 0, 0, 0.5);
    pointer-events: none;
    transform: translateX(-50%);
    z-index: 10;
  }

  .split-drag-handle {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 32px;
    height: 32px;
    border-radius: var(--radius-full);
    background: #ffffff;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    display: flex;
    align-items: center;
    justify-content: center;
    color: #0f172a;
    gap: 0px;
  }

  .split-tag {
    position: absolute;
    bottom: 10px;
    background: rgba(15, 23, 42, 0.75);
    backdrop-filter: blur(4px);
    color: #ffffff;
    font-size: 10.5px;
    font-weight: 700;
    padding: 3px 8px;
    border-radius: var(--radius-sm);
    pointer-events: none;
  }

  .split-tag.tag-left {
    left: 10px;
  }

  .split-tag.tag-right {
    right: 10px;
  }

  .split-hint-text {
    font-size: 11px;
    color: var(--color-text-muted);
    margin: 0;
  }

  /* Side-by-Side */
  .side-viewport-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3);
    width: 100%;
  }

  .side-box {
    position: relative;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: var(--color-surface-subtle);
  }

  .side-badge {
    position: absolute;
    top: 8px;
    left: 8px;
    background: rgba(15, 23, 42, 0.75);
    backdrop-filter: blur(4px);
    color: #ffffff;
    font-size: 10px;
    font-weight: 700;
    padding: 2px 6px;
    border-radius: var(--radius-sm);
  }

  .side-img {
    width: 100%;
    height: auto;
    display: block;
  }

  /* Single Result */
  .single-viewport-wrapper {
    width: 100%;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
  }

  .single-img {
    width: 100%;
    height: auto;
    display: block;
  }

  /* Controls Panel (Right Column) */
  .gray-controls-panel {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--space-4);
    box-shadow: var(--shadow-sm);
    display: flex;
    flex-direction: column;
    gap: var(--space-3-5);
    box-sizing: border-box;
  }

  @media (min-width: 640px) {
    .gray-controls-panel {
      padding: var(--space-5);
    }
  }

  .control-group {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .control-label {
    font-size: var(--text-xs);
    font-weight: 700;
    color: var(--color-text);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  .slider-header-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .val-pill {
    font-size: 11px;
    font-family: monospace;
    font-weight: 700;
    color: var(--color-text);
    background: var(--color-surface-subtle);
    border: 1px solid var(--color-border);
    padding: 1px 6px;
    border-radius: var(--radius-sm);
  }

  .gray-range {
    width: 100%;
    accent-color: var(--color-accent);
  }

  /* Presets */
  .preset-chips-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-1-5);
  }

  @media (min-width: 480px) {
    .preset-chips-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .tone-preset-btn {
    background: var(--color-surface-subtle);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-2) var(--space-2-5);
    text-align: left;
    cursor: pointer;
    display: flex;
    flex-direction: column;
    gap: 2px;
    transition: all 0.15s ease;
  }

  .tone-preset-btn:hover {
    border-color: var(--color-accent-border);
  }

  .tone-preset-btn.active {
    background: var(--color-accent-light);
    border-color: var(--color-accent);
    box-shadow: 0 0 0 1px var(--color-accent);
  }

  .tone-preset-btn strong {
    font-size: 11.5px;
    font-weight: 700;
    color: var(--color-text);
  }

  .tone-preset-btn span {
    font-size: 10.5px;
    color: var(--color-text-secondary);
  }

  .reset-adjustments-row {
    display: flex;
    justify-content: flex-end;
  }

  .gray-select {
    width: 100%;
    padding: 8px 12px;
    font-size: var(--text-xs);
    font-weight: 600;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-surface);
    color: var(--color-text);
  }

  /* Metrics Card */
  .gray-metrics-card {
    background: var(--color-surface-subtle);
    border: 1px solid var(--color-border-light);
    border-radius: var(--radius-lg);
    padding: var(--space-3);
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .metric-row {
    display: flex;
    justify-content: space-between;
    font-size: 11.5px;
    color: var(--color-text-secondary);
  }

  .metric-row strong {
    color: var(--color-text);
  }

  .gray-download-btn {
    width: 100%;
    min-height: 46px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    font-size: var(--text-sm);
    font-weight: 700;
  }

  /* Cross Links */
  .gray-cross-links-row {
    display: flex;
    flex-direction: column;
    gap: var(--space-2-5);
    margin-top: var(--space-2);
  }

  @media (min-width: 600px) {
    .gray-cross-links-row {
      flex-direction: row;
      justify-content: center;
      flex-wrap: wrap;
    }
  }

  .gray-cross-btn {
    min-height: 42px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 0 var(--space-4);
    font-size: var(--text-xs);
    font-weight: 700;
  }
</style>
